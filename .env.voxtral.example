# For local/dev experiments only (NEVER commit real secrets)

# --- Backend relay side (secret) ---
MISTRAL_API_BASE_URL=https://api.mistral.ai
MISTRAL_MODEL=voxtral-mini-latest
MISTRAL_API_KEY=replace-me
VOXTRAL_RELAY_AUTH_TOKEN=replace-me

# --- Keyboard app side (non-secret endpoint config) ---
VOXTRAL_RELAY_BASE_URL=https://your-relay.example.com
VOXTRAL_RELAY_CLIENT_TOKEN=
